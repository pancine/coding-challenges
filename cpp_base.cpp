// #include <bits/stdc++.h>
#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

#define ibs ios_base::sync_with_stdio(false);
#define cti cin.tie(NULL);
#define endl "\n"

int main()
{
    cti;
    ibs;
    int test_cases;
    cin >> test_cases;

    return 0;
}

#define coutMatrix(matrix) for(size_t i=0;i<matrix.size();i++){for(size_t j=0;j<matrix[i].size();j++){cout<<matrix[i][j]<<" ";}cout<<"\n";}
#define coutVector(vector) for(size_t i=0;i<vector.size();i++){cout<<vector[i]<<" ";}cout<<"\n";